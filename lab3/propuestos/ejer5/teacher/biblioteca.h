#include <bits/stdc++>

class Sala{
    private:    
        int ancho, largo;
        bool equipado;
        char* nombre;
    public:
        Sala(char* n, int a, int l, bool e);
        char* getNombre();
        
}